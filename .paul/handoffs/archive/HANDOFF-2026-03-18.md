# PAUL Handoff

**Date:** 2026-03-18
**Status:** paused

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** mdb-esp32-cashless — Open-source MDB telemetry system for vending machines (ESP32-S3 firmware + self-hosted Docker backend + Nuxt 4 dashboard)
**Core value:** Vending machine operators can manage all the telemetry, monitor sales, and optimize inventory from a single dashboard

---

## Current State

**Milestone:** AI Insights & Optimization (v1.1)
**Phase:** 2 of 3 — insights-edge-function
**Plan:** 02-01 — executed (APPLY complete)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○     ← needs UNIFY to close the loop
```

---

## What Was Done This Session

- **Phase 02, Plan 02-01 APPLIED in full:**
  - Created `Docker/supabase/functions/machine-insights/index.ts` — Deno edge function with dual auth (JWT + X-API-Key), RPC call to `get_machine_insights_kpis`, Anthropic `claude-haiku-4-5` API call, structured JSON response
  - Created `Docker/supabase/functions/machine-insights/deno.json` — import map for `@supabase/supabase-js` and `@anthropic-ai/sdk`
  - Updated `Docker/supabase/config.toml` — added `[functions.machine-insights]` block and `ANTHROPIC_API_KEY` in `[edge_runtime.secrets]`
  - Updated `Docker/.env.example` — added `ANTHROPIC_API_KEY=` placeholder after `OPENAI_API_KEY`
  - Updated `Docker/supabase/.env.example` — added `ANTHROPIC_API_KEY=` at bottom
  - Updated `Docker/setup.sh` — added warn-only block + writes `ANTHROPIC_API_KEY` to output `.env`
  - Updated `Docker/update.sh` — added warn-only check for missing key
  - Created `.paul/phases/02-insights-edge-function/02-01-SUMMARY.md`

---

## What's In Progress

- **UNIFY** — needs to be run to reconcile plan vs actual and close Phase 02 loop
  - No deviations from plan — all tasks executed exactly as specified

---

## What's Next

**Immediate:** Run `/paul:unify .paul/phases/02-insights-edge-function/02-01-PLAN.md` to close Phase 02 loop

**After that:** Phase 03 — insights-ui (`useInsights` composable + "AI Insights" button on `/machines/[id]` that calls the edge function and shows recommendations in a sheet/modal)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (3 phases) |
| `.paul/phases/02-insights-edge-function/02-01-PLAN.md` | Executed plan |
| `.paul/phases/02-insights-edge-function/02-01-SUMMARY.md` | Implementation notes + Phase 03 integration guide |
| `Docker/supabase/functions/machine-insights/index.ts` | The edge function just built |

---

## Critical Context to Remember

- **Auth pattern:** Dual auth (X-API-Key first, then JWT Bearer) — matches `send-credit/index.ts` exactly
- **RPC:** `get_machine_insights_kpis(p_machine_id, p_company_id, p_days)` — Phase 01 artifact, do not modify
- **Model:** `claude-haiku-4-5` — fast/cheap, returns raw JSON (no markdown wrapping)
- **ANTHROPIC_API_KEY:** runtime-only — NOT in Dockerfile/docker-compose build args
- **Schema FK:** `company` column (not `company_id`) on vendingMachine, embeddeds, products
- **`supabase db reset` broken locally** — apply migrations via `docker exec supabase_db_supabase-test psql -U postgres -d postgres < <file>`

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:unify .paul/phases/02-insights-edge-function/02-01-PLAN.md` to close Phase 02
3. Then `/paul:progress` to confirm readiness for Phase 03

---

*Handoff created: 2026-03-18*
